# Terraform state (never commit these)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
terraform.tfvars        # Contains your project ID and email

# Secrets
*.pem
*.key
service-account*.json

# OS
.DS_Store
*.swp
